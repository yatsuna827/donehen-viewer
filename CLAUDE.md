
- functionではなくアロー関数を使う
- コンポーネントは `React.FC` で型を付ける
- 型には`readonly`は付けないのを基本とする。`readonly`になっていなかろうがミュータブルなコードは書かない。
- pnpmプロジェクトだからnpxは使うな
- 型チェックとlintとformatとtestは回す ビルドは不要
  - それぞれ別で実行する。`&&`で繋げることはしない。
